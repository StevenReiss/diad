<QUERIES>
   <QUERY COMMAND='EXPLAIN'>
      You should return an explanation of the root cause of the problem
      that led to the symptom.	You should start with the methods that are
      returned by getFaultLocations, examine the trace for those methods (you
      can get the callids using getCallIdsForMethod and the actual trace using
      getCallTrace) and determine how this trace is relevant to the symptom
      $(SYMPTOM).
   </QUERY>
   <QUERY COMMAND='BASEEXPLAIN'>
      You should return an explanation of the root cause of the problem
      that led to the symptom.	You should start with the stack trace (using
      the tool getStackFrames) and can evaluate any expression that will help
      (using getEvaluation).  You should provide an explanation that is relevant
      the the symptom
      $(SYMPTOM).
   </QUERY>
   <QUERY COMMAND='RETRY'>
      That was either a bad explanation (i.e., you are telling what to do rather
      than doing it yourself), or it is incorrect or incomplete.  Please try
      again and do a better job.
   </QUERY>
   <QUERY COMMAND='TEST1'>
      You should return a detailed explanation of the root cause of the problem
      that led to the symptom.	You should explore
      the call trace using the 'getCallTrace' agent to explore the execution
      leading to the problem and provide an explanation in terms of this execution.
   </QUERY>
   <QUERY COMMAND='TEST2'>
      You should return an explanation of the root cause of the problem
      that led to the symptom.	You should start with the methods that are
      returned by getFaultLocations, examine the trace for those methods (you
      can get the callids using getCallIdsForMethod and the actual trace using
      getCallTrace) and determine how this trace is relevant to the symptom
      $(SYMPTOM).
   </QUERY>
   <QUERY COMMAND='TEST3'>
      You should invoke the agents directly to get the information needed.  You should
      not respond with what agents to call or what to do to get the answer.

      You should return three possible explanations of the root cause of the problem
      that led to the symptom.	This should be an analysis that starts
      with the symptom and indicates what in the program caused it.  It
      can be specific, identifying the lines that led to the problem,
      or can be general, indicating the overall problem and what types of
      things to look for.
      For example, if the fault is a NullPointerException, explain how
      the particular value could be null at the stopping point.
   </QUERY>
   <QUERY COMMAND='TEST4'>
      You should return an explanation of the root cause of the problem
      that led to the symptom.	This should be an analysis that starts
      with the symptom and indicates what in the program caused it.  It
      can be specific, identifying the lines that led to the problem,
      or can be general, indicating the overall problem and what types of
      things to look for.
      For example, if the fault is a NullPointerException, explain how
      the particular value could be null at the stopping point.
   </QUERY>
   <QUERY COMMAND='TEST5'>
      Your job here is to identify the most likely repairs to the
      program that will fix the problem.   Tools are provided that identify the
      overall set of locations, but these are not prioritized or evaluated
      for appropriateness and do not provide fixes.  You should consider
      these lines, analyze whether they are likely to be buggy, determine
      if and how they might be changed.  This should be based on your understanding
      of the cause of the problem and your knowledge of the program and
      programming in general.
      In the end you should provide a set of possible patches
      that are likely to fix the problem.  Providing multiple alternatives
      is fine.	Also provide the patches in a format suitable for the Linux patch command.
   </QUERY>
   <QUERY COMMAND='TEST6'>
      Your should identify the most likely program locations that can
      be repaired to fix the problem.	Tools are provided that identify the
      set of locations that might affect the problem, but these are not prioritized or evaluated
      for appropriateness.  Given your understanding of the cause of the
      problem you should determine which of these locations are the most
      likely to be buggy.
   </QUERY>
   <QUERY COMMAND='LOCATIONS'>
      That seems correct.  Please provide a set of relevant source locations based on that
      in either the form ```LOC!!filename!!line``` or ```LOC!!methodname!!line```
      where filename is the full name of the relevant source file, methodname
      is the method name [class.method], and line is a relevant line number.
   </QUERY>
   <QUERY COMMAND='REPAIRS'>
      That analysis seems correct.
      Please provide the necessary patches based on that as input to the Linux patch command.
      Do not call a patch agent, just return the patches.
   </QUERY>
   <QUERY COMMAND='USER'>
   </QUERY>
</QUERIES>








